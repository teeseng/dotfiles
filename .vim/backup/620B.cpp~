#include <iostream>
#include <cmath> 
using namespace std;

void print_each_digit(int x) {
    if(x >= 10) 
        print_each_digit(x/10);
    int digit = x % 10;
          
}

int main() {
   int start, end; 
   cin >> start >> end; 

   for(;start < end; start++) {
        
   }
    

}
