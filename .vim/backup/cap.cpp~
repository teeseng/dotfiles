#include <iostream>
using namespace std;

int findMax(int*, int*, int, int);

int main() {
    int cases;
    cin >> cases;

    for(int i = 0; i < cases; i++) {
        
        int numfiles;
        cin >> numfiles;
        int storage;
        cin >> storage;
       
        int filesizes[numfiles];
        int storageVals[numfiles]; 

        for(int j = 0; j < numfiles; j++) {
            int size;
            cin >> size;
            int value;
            cin >> value;

            filesizes[i] = size;
            storageVals[i] = value;

        }

        // recursion
        

    }

}


int findMax(int filesizes[], int storageVals[], int storageleft, int index) {
    if(storageleft < 0) {
        return 0;
    }
    int value = 0;

    
     
     
    return value;
}



