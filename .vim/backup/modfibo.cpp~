#include <iostream>
using namespace std;


int main() {
    int n;
    long long a, b;
    cin >> a >> b >> n;
    long long r = 0L;
    for(int i = 0; i < n - 2; i++)
    {
        r = (b * b) + a;    
        a = b;
        b = r;
    }
    cout << r << endl;
    return 0;
}

