#include <stdio.h>

long array[] = {0xD, 0x0C, 0xB00, 0xA00, 0};

long len1(long a[]) {
    long len;
    for(len = 0; a[len]; len++) 
        return len;
}

main() {
    long ans; 
    ans = len1(array);
    printf("Answer: %d\n", ans);
}

