#include <iostream>
using namespace std;

const int size = 10;

int f(int a[], int size) {
    for (int i = 0; i < sizeof(size); i++) {
        // initializing stuff
    }
    return 0;    
}

int main(int argc, char const *argv[]) {
    int a[size];
    for(int i = 0; i < size; i++) {  a[i] = 0; } // initializes everything to 0
    return 0;

    int a2[size] = {1, 2, 3, 4}; // assumes the rest of the 6 are zero so: [1, 2, 3, 4, 0 ... 0 ]
    
    char word[] = "hello";
    size_t t;
    cout << sizeof t << endl; // sizeof t compiles and works the way you think it does.
    
    size_t j = 0;
    for(size_t j = 0; j < 10; j++) {
              
    }
}

