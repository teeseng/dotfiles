#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>
int main() {
    int i = 0;

    pid_t pid = fork();
    if(pid == 0) {
        i = i - 1;
        return 0;
    } else {
        int status;
        waitpid(-1, &status, WNOHANG);

        i = i + 1;
        printf("i = %d\n", i);
    }
}
