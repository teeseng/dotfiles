#include <iostream>
#include <string.h>
using namespace std;

int main() {
    int cases;
    cin >> cases;
    char key[8] = "Dropbox";

    for(int i = 0; i < cases; i++) {
       char word[8];
       cin >> word;
       if(strcmp(key, word) == 0) {
           cout << "YES" << endl;
       } else {
           cout << "NO" << endl;
       } 
    }

    return 0;

}
