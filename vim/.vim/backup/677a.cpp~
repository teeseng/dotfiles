#include <iostream>
using namespace std;

int main()
{
    int n, h;
    cin >> n >> h;
    int r = 0;
    int a;

    for(int i = 0; i < n; i++)
    {
        cin >> a;
        if(a <= h)
            r += 1;
        else 
            r += 2;
    }
    cout << r << endl; 

    return 0;
}
